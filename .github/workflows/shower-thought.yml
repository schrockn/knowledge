name: Create Shower Thought Document

on:
  issues:
    types: [opened]

jobs:
  create-shower-thought:
    if: contains(github.event.issue.title, '[shower-thought]')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Create shower thought document
      run: python .github/scripts/create_shower_thought.py
      env:
        ISSUE_TITLE: ${{ github.event.issue.title }}
        ISSUE_BODY: ${{ github.event.issue.body }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add shower-thoughts/
        if ! git diff --staged --quiet; then
          git commit -m "Add shower thought from issue #${{ github.event.issue.number }}"
          git push
          echo "CHANGES_COMMITTED=true" >> $GITHUB_ENV
        fi
        
    - name: Close issue
      if: env.CHANGES_COMMITTED == 'true'
      run: |
        gh issue close ${{ github.event.issue.number }} --comment "Shower thought has been processed and committed. Thank you!"
      env:
        GH_TOKEN: ${{ github.token }}